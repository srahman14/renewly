"use client";

import { useMemo, useState } from "react";

const services = [
  { name: "Netflix", price: 17.99, mark: "N", colour: "bg-red-500" },
  { name: "Spotify", price: 11.99, mark: "S", colour: "bg-emerald-500" },
  { name: "Adobe CC", price: 56.98, mark: "A", colour: "bg-orange-500" },
];

const faqs = [
  ["Do I need to connect my bank?", "Never. Add subscriptions yourself, pick from a service catalogue, or import a CSV. Your financial accounts remain completely separate."],
  ["Will I be reminded before a trial turns paid?", "Yes. Set the reminder window you want and Renewly will surface trials, renewals and price changes before they cost you."],
  ["Is Renewly a monthly subscription?", "No. Start free with five subscriptions, then unlock unlimited tracking with a one-time lifetime purchase."],
  ["Where does my data go?", "We built Renewly to be private by default. We do not sell your data or ask for access to your bank or inbox."],
];

export default function Home() {
  const [dashboardView, setDashboardView] = useState<"overview" | "calendar">("overview");
  const [noticeDays, setNoticeDays] = useState<30 | 60>(30);
  const [selectedService, setSelectedService] = useState<(typeof services)[number] | null>(null);
  const [decision, setDecision] = useState<"keep" | "cancel" | null>(null);
  const [openFaq, setOpenFaq] = useState(0);
  const [toast, setToast] = useState(false);
  const monthlyTotal = useMemo(() => services.reduce((total, service) => total + service.price, 0), []);

  function begin() {
    setToast(true);
    window.setTimeout(() => setToast(false), 2500);
  }

  return (
    <main className="min-h-screen overflow-hidden bg-[#09090b] text-zinc-100 selection:bg-lime-300 selection:text-zinc-950">
      <style>{`@keyframes pageFloat{0%,100%{transform:translateY(0) rotate(-1deg)}50%{transform:translateY(-10px) rotate(1deg)}}@keyframes glow{0%,100%{opacity:.3}50%{opacity:.8}}.page-float{animation:pageFloat 6s ease-in-out infinite}.page-glow{animation:glow 3s ease-in-out infinite}`}</style>
      <div className="pointer-events-none fixed inset-0 bg-[radial-gradient(circle_at_50%_-15%,rgba(163,230,53,.17),transparent_34%),radial-gradient(circle_at_5%_55%,rgba(34,197,94,.08),transparent_24%)]" />

      {/* Navbar intentionally omitted — use your existing Navbar component above this page. */}
      <section id="top" className="relative mx-auto grid max-w-7xl gap-12 px-5 pb-20 pt-16 sm:px-8 sm:pt-20 lg:grid-cols-[.88fr_1.12fr] lg:items-center lg:gap-14 lg:px-10 lg:pb-32 lg:pt-28">
        <div className="relative z-10 mx-auto max-w-2xl text-center lg:mx-0 lg:text-left">
          <div className="inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/5 px-3 py-1.5 text-xs text-zinc-300 backdrop-blur"><span className="page-glow size-1.5 rounded-full bg-lime-300" /> Your money deserves a heads-up</div>
          <h1 className="mt-6 text-balance text-[clamp(3.2rem,10vw,6.5rem)] font-semibold leading-[.91] tracking-[-.07em] text-white">Every renewal.<br /><span className="text-lime-300">Your decision.</span></h1>
          <p className="mx-auto mt-7 max-w-lg text-base leading-relaxed text-zinc-400 sm:text-lg lg:mx-0">Renewly turns invisible recurring costs into clear choices. Know what is coming, what it costs, and what is worth keeping—before your card gets charged.</p>
          <div className="mt-8 flex flex-col justify-center gap-3 sm:flex-row lg:justify-start"><button onClick={begin} className="rounded-full bg-white px-5 py-3.5 text-sm font-semibold text-zinc-950 transition hover:bg-lime-200 active:scale-95">Build my dashboard <span aria-hidden>↗</span></button><a href="#how-it-works" className="rounded-full border border-white/15 px-5 py-3.5 text-sm font-medium text-white transition hover:border-white/40 hover:bg-white/5">See it in action</a></div>
          <p className="mt-4 text-xs text-zinc-500">No bank login. No inbox access. No awkward sales pitch.</p>
        </div>

        <div className="relative mx-auto w-full max-w-xl lg:max-w-2xl">
          <div className="absolute -left-8 top-12 size-48 rounded-full bg-lime-300/20 blur-3xl" />
          <DashboardPreview view={dashboardView} setView={setDashboardView} total={monthlyTotal} />
          <div className="page-float absolute -right-2 -top-5 hidden rounded-2xl border border-white/10 bg-zinc-900/95 p-3 shadow-xl backdrop-blur sm:block lg:-right-7"><p className="text-[10px] text-zinc-400">TRIAL ENDING</p><p className="mt-1 text-sm font-semibold">iCloud+ in 2 days</p><p className="mt-2 text-[10px] text-lime-300">● You&apos;re on it</p></div>
          <div className="absolute -bottom-6 -left-2 hidden rounded-2xl border border-white/10 bg-white p-3 text-zinc-950 shadow-xl sm:block lg:-left-7"><p className="text-[10px] font-medium text-zinc-500">SAVED THIS YEAR</p><p className="mt-1 text-2xl font-bold tracking-tight">£311.20</p></div>
        </div>
      </section>

      <section className="border-y border-white/10 bg-white/[.025]"><div className="mx-auto grid max-w-7xl grid-cols-2 divide-x divide-white/10 sm:grid-cols-4"><Stat value="£500+" label="forgotten yearly spend" /><Stat value="30 sec" label="to add a renewal" /><Stat value="0" label="bank logins required" /><Stat value="1×" label="price, yours forever" /></div></section>

      <section id="purpose" className="mx-auto max-w-7xl px-5 py-20 sm:px-8 sm:py-28 lg:px-10"><div className="grid gap-8 lg:grid-cols-[.8fr_1.2fr] lg:gap-20"><div><p className="text-sm font-medium text-lime-300">The anti-subscription subscription.</p><h2 className="mt-4 text-balance text-4xl font-semibold tracking-[-.06em] sm:text-5xl">A calmer relationship with recurring money.</h2></div><p className="max-w-2xl self-end text-base leading-relaxed text-zinc-400 sm:text-lg">Most trackers demand access to more of your life. Renewly does the opposite: it gives you one considered place for the details that matter, then gets out of the way until you need it.</p></div><div className="mt-12 grid gap-px overflow-hidden rounded-2xl border border-white/10 bg-white/10 md:grid-cols-3"><Purpose title="See the real total" copy="Turn a harmless-looking monthly payment into its real yearly cost." /><Purpose title="Catch the quiet ones" copy="Trials and price rises become a decision, not an unpleasant surprise." highlight /><Purpose title="Cancel with context" copy="Keep the account details and cancel link you will need later." /></div></section>

      <section id="how-it-works" className="border-y border-white/10 bg-[#0e0e10] py-20 sm:py-28"><div className="mx-auto max-w-7xl px-5 sm:px-8 lg:px-10"><div className="max-w-2xl"><p className="text-sm font-medium text-lime-300">See the workflow</p><h2 className="mt-4 text-balance text-4xl font-semibold tracking-[-.06em] sm:text-5xl">Three small moves. A much clearer picture.</h2><p className="mt-5 text-zinc-400">These are miniature, usable versions of the moments inside your dashboard. Try them.</p></div><div className="mt-12 grid gap-5 lg:grid-cols-3"><AddSubscriptionCard selected={selectedService} onSelect={setSelectedService} /><DeadlineCard noticeDays={noticeDays} setNoticeDays={setNoticeDays} /><DecisionCard decision={decision} setDecision={setDecision} /></div></div></section>

      <section id="faq" className="mx-auto max-w-4xl px-5 py-20 sm:px-8 sm:py-28"><div className="text-center"><p className="text-sm font-medium text-lime-300">Good questions</p><h2 className="mt-4 text-4xl font-semibold tracking-[-.06em] sm:text-5xl">Nothing hidden here.</h2></div><div className="mt-12 divide-y divide-white/10 border-y border-white/10">{faqs.map(([question, answer], index) => <div key={question}><button onClick={() => setOpenFaq(openFaq === index ? -1 : index)} className="flex w-full items-center justify-between gap-5 py-5 text-left text-base font-medium sm:text-lg"><span>{question}</span><span className="shrink-0 text-xl text-lime-300">{openFaq === index ? "−" : "+"}</span></button>{openFaq === index && <p className="max-w-2xl pb-5 pr-4 text-sm leading-relaxed text-zinc-400">{answer}</p>}</div>)}</div></section>

      <section className="mx-3 mb-3 overflow-hidden rounded-3xl bg-lime-300 text-zinc-950 sm:mx-6"><div className="relative mx-auto max-w-7xl px-5 py-16 text-center sm:px-8 sm:py-24"><span className="absolute left-[6%] top-2 text-7xl font-black opacity-10">£</span><span className="absolute bottom-0 right-[8%] text-9xl font-black opacity-10">R</span><p className="text-xs font-semibold sm:text-sm">NO BANK LOGIN · NO MONTHLY FEE</p><h2 className="mx-auto mt-5 max-w-3xl text-balance text-4xl font-semibold tracking-[-.065em] sm:text-6xl">Make your next renewal an intentional one.</h2><button onClick={begin} className="mt-8 rounded-full bg-zinc-950 px-6 py-3.5 text-sm font-semibold text-white transition hover:bg-zinc-800 active:scale-95">Start tracking for free <span aria-hidden>→</span></button></div></section>
      {/* Footer intentionally omitted — use your existing Footer component below this page. */}
      {toast && <div role="status" className="fixed bottom-5 left-1/2 z-50 w-max max-w-[calc(100%-2rem)] -translate-x-1/2 rounded-full border border-lime-300/30 bg-zinc-900 px-4 py-3 text-center text-sm shadow-2xl">Your fresh dashboard is ready to build <span className="text-lime-300">→</span></div>}
    </main>
  );
}

function DashboardPreview({ view, setView, total }: { view: "overview" | "calendar"; setView: (view: "overview" | "calendar") => void; total: number }) { return <div className="relative overflow-hidden rounded-[1.65rem] border border-white/10 bg-[#151518] p-2.5 shadow-2xl shadow-black/50 sm:rounded-[2rem] sm:p-3"><div className="flex items-center justify-between rounded-xl border border-white/5 bg-[#1b1b1f] px-3 py-2.5 text-[10px] text-zinc-500 sm:px-4 sm:text-xs"><div className="flex gap-1.5"><i className="size-2 rounded-full bg-rose-400/70" /><i className="size-2 rounded-full bg-amber-300/70" /><i className="size-2 rounded-full bg-emerald-400/70" /></div><span>renewly.app / dashboard</span><span>•••</span></div><div className="mt-3 rounded-xl bg-[#101012] p-4 sm:rounded-2xl sm:p-6"><div className="flex items-start justify-between gap-3"><div><p className="text-[10px] text-zinc-500 sm:text-xs">Good morning, Alex</p><h3 className="mt-1 text-base font-semibold tracking-tight sm:text-xl">Your money, in focus.</h3></div><button onClick={() => setView(view === "overview" ? "calendar" : "overview")} className="rounded-lg border border-white/10 bg-white/5 px-2.5 py-2 text-[10px] text-zinc-300 transition hover:bg-white/10 sm:text-xs">{view === "overview" ? "Calendar ↗" : "Overview ←"}</button></div>{view === "overview" ? <><div className="mt-5 grid grid-cols-2 gap-2.5 sm:mt-7 sm:gap-3"><div className="rounded-xl bg-lime-300 p-3 text-zinc-950 sm:p-4"><p className="text-[10px] font-medium opacity-60 sm:text-xs">Monthly spend</p><p className="mt-2 text-2xl font-semibold tracking-tight sm:text-3xl">£{total.toFixed(2)}</p><p className="mt-2 text-[10px] font-medium sm:mt-3 sm:text-xs">14 active subscriptions</p></div><div className="rounded-xl border border-white/10 bg-white/[.03] p-3 sm:p-4"><p className="text-[10px] text-zinc-500 sm:text-xs">You&apos;ve kept</p><p className="mt-2 text-2xl font-semibold tracking-tight sm:text-3xl">£311</p><p className="mt-2 text-[10px] text-emerald-300 sm:mt-3 sm:text-xs">↑ £74 this month</p></div></div><div className="mt-3 rounded-xl border border-white/10 bg-white/[.025] p-3 sm:mt-4 sm:p-4"><div className="flex justify-between text-[10px] sm:text-xs"><span className="text-zinc-400">Monthly budget</span><span>£87 <span className="text-zinc-500">/ £120</span></span></div><div className="mt-2.5 h-1.5 overflow-hidden rounded-full bg-white/10"><div className="h-full w-[72%] rounded-full bg-gradient-to-r from-lime-300 to-emerald-400" /></div></div><div className="mt-3 space-y-1.5 sm:mt-4">{services.map((service, index) => <div key={service.name} className="flex items-center gap-2.5 rounded-lg border border-white/5 bg-white/[.025] px-2.5 py-2 sm:px-3"><span className={`grid size-5 place-items-center rounded text-[9px] font-bold ${service.colour}`}>{service.mark}</span><span className="flex-1 text-[10px] font-medium sm:text-xs">{service.name}</span><span className={`text-[9px] ${index === 0 ? "text-rose-300" : "text-zinc-500"}`}>{index === 0 ? "Tomorrow" : `In ${index * 4} days`}</span><span className="text-[10px] sm:text-xs">£{service.price}</span></div>)}</div></> : <div className="mt-6 grid grid-cols-7 gap-1.5 sm:gap-2">{Array.from({ length: 28 }, (_, index) => <div key={index} className={`grid aspect-square place-items-center rounded-md text-[9px] sm:rounded-lg sm:text-xs ${[5, 12, 21].includes(index) ? "bg-lime-300 font-bold text-zinc-950" : "bg-white/[.04] text-zinc-400"}`}>{index + 1}</div>)}<p className="col-span-7 mt-1 text-[10px] text-zinc-500"><span className="mr-1 inline-block size-1.5 rounded-full bg-lime-300" />Renewals marked in lime</p></div>}</div></div> }

function AddSubscriptionCard({ selected, onSelect }: { selected: (typeof services)[number] | null; onSelect: (service: (typeof services)[number]) => void }) { return <article className="overflow-hidden rounded-2xl border border-white/10 bg-[#151518]"><div className="border-b border-white/10 p-5"><p className="text-xs font-medium text-lime-300">01 — ADD</p><h3 className="mt-2 text-xl font-semibold">Start with the one you remember.</h3><p className="mt-2 text-sm leading-relaxed text-zinc-400">Pick a service below to add it to your dashboard.</p></div><div className="p-4"><div className="rounded-lg border border-white/10 bg-black/20 px-3 py-2.5 text-xs text-zinc-500">⌕ Search 40+ popular services</div><div className="mt-3 grid grid-cols-3 gap-2">{services.map((service) => <button key={service.name} onClick={() => onSelect(service)} className={`rounded-xl border p-2 text-left transition ${selected?.name === service.name ? "border-lime-300 bg-lime-300/10" : "border-white/10 bg-white/[.03] hover:bg-white/[.08]"}`}><span className={`grid size-6 place-items-center rounded text-[10px] font-bold ${service.colour}`}>{service.mark}</span><p className="mt-4 truncate text-xs font-medium">{service.name}</p><p className="mt-1 text-[10px] text-zinc-500">£{service.price}/mo</p></button>)}</div>{selected ? <div className="mt-3 flex items-center gap-2 rounded-xl bg-emerald-400/10 px-3 py-2.5 text-xs text-emerald-200"><span>✓</span> {selected.name} added to July</div> : <p className="mt-3 text-center text-[11px] text-zinc-600">Choose a service to try it</p>}</div></article> }
function DeadlineCard({ noticeDays, setNoticeDays }: { noticeDays: 30 | 60; setNoticeDays: (days: 30 | 60) => void }) { const date = noticeDays === 30 ? "14 Aug" : "15 Jul"; return <article className="overflow-hidden rounded-2xl border border-lime-300/30 bg-lime-300 text-zinc-950"><div className="p-5"><p className="text-xs font-medium text-zinc-700">02 — REMIND</p><h3 className="mt-2 text-xl font-semibold">Give yourself a runway.</h3><p className="mt-2 text-sm leading-relaxed text-zinc-700">Choose when Renewly should step in.</p></div><div className="mx-4 rounded-xl bg-zinc-950 p-4 text-white"><div className="flex items-center justify-between text-xs text-zinc-400"><span>Adobe Creative Cloud</span><span>Renews 13 Sep</span></div><p className="mt-5 text-[10px] font-medium uppercase tracking-wider text-lime-300">Last safe reminder</p><p className="mt-1 text-3xl font-semibold tracking-tight">{date}</p><p className="mt-1 text-xs text-zinc-400">{noticeDays} days before renewal</p></div><div className="p-4"><p className="mb-2 text-[11px] font-medium text-zinc-700">REMIND ME</p><div className="flex rounded-lg bg-black/10 p-1">{([30, 60] as const).map((days) => <button key={days} onClick={() => setNoticeDays(days)} className={`flex-1 rounded-md py-2 text-xs font-semibold transition ${noticeDays === days ? "bg-zinc-950 text-white shadow" : "text-zinc-700 hover:bg-white/20"}`}>{days} days before</button>)}</div></div></article> }
function DecisionCard({ decision, setDecision }: { decision: "keep" | "cancel" | null; setDecision: (decision: "keep" | "cancel") => void }) { return <article className="overflow-hidden rounded-2xl border border-white/10 bg-[#151518]"><div className="border-b border-white/10 p-5"><p className="text-xs font-medium text-lime-300">03 — DECIDE</p><h3 className="mt-2 text-xl font-semibold">Keep it. Or make room.</h3><p className="mt-2 text-sm leading-relaxed text-zinc-400">A reminder is only useful when it gives you a choice.</p></div><div className="p-4"><div className="rounded-xl border border-white/10 bg-white/[.03] p-3"><div className="flex items-center gap-3"><span className="grid size-9 place-items-center rounded-lg bg-red-500 text-xs font-bold">N</span><div className="flex-1"><p className="text-sm font-semibold">Netflix Premium</p><p className="text-xs text-zinc-500">Renews tomorrow · £17.99</p></div></div><div className="mt-4 flex gap-2"><button onClick={() => setDecision("keep")} className={`flex-1 rounded-lg border py-2 text-xs font-medium transition ${decision === "keep" ? "border-lime-300 bg-lime-300 text-zinc-950" : "border-white/10 hover:bg-white/10"}`}>Keep it</button><button onClick={() => setDecision("cancel")} className={`flex-1 rounded-lg border py-2 text-xs font-medium transition ${decision === "cancel" ? "border-rose-400 bg-rose-400 text-zinc-950" : "border-white/10 hover:bg-white/10"}`}>Cancel</button></div></div><div className={`mt-3 rounded-xl px-3 py-3 text-xs transition ${decision === "cancel" ? "bg-emerald-400/10 text-emerald-200" : decision === "keep" ? "bg-white/[.04] text-zinc-300" : "bg-black/20 text-zinc-500"}`}>{decision === "cancel" ? "£215.88 stays in your pocket this year." : decision === "keep" ? "Kept. We will bring this back next renewal." : "Make a choice to see the impact."}</div></div></article> }
function Purpose({ title, copy, highlight = false }: { title: string; copy: string; highlight?: boolean }) { return <article className={`min-h-52 p-6 ${highlight ? "bg-lime-300 text-zinc-950" : "bg-[#151518]"}`}><span className={`text-3xl ${highlight ? "text-zinc-800" : "text-lime-300"}`}>↗</span><h3 className="mt-10 text-xl font-semibold tracking-tight">{title}</h3><p className={`mt-3 max-w-xs text-sm leading-relaxed ${highlight ? "text-zinc-700" : "text-zinc-400"}`}>{copy}</p></article> }
function Stat({ value, label }: { value: string; label: string }) { return <div className="px-3 py-6 text-center sm:px-6 sm:py-8"><p className="text-xl font-semibold tracking-tight text-white sm:text-3xl">{value}</p><p className="mt-1 text-[10px] text-zinc-500 sm:text-xs">{label}</p></div> }
